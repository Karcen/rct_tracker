# 运行依赖（Tkinter 与 sqlite3 为 Python 标准库，无需在此列出）
# Runtime dependencies (Tkinter and sqlite3 are stdlib, not listed here)

python-barcode>=0.15.1   # 生成 Code 128 一维码 / generate Code 128 barcodes
Pillow>=10.0.0           # 条码图片渲染与预览 / render & preview barcode images
openpyxl>=3.1.0          # 导出 Excel 台账 / export Excel ledger

# 打包工具（可选，仅打包 exe 时需要）/ Packaging tool (optional, only for building exe)
# pyinstaller>=6.0.0
